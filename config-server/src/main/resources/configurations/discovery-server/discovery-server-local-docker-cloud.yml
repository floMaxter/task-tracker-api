server:
  port: ${DISCOVERY_SERVER_PORT:8761}

eureka:
  instance:
    hostname: ${DISCOVERY_SERVER_HOST:task-tracker-discovery-server}
  client:
    register-with-eureka: false
    fetch-registry: false