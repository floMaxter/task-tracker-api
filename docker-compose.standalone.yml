services:
  user-postgres-db:
    image: postgres:15-alpine
    container_name: task-tracker-user-db
    env_file:
      - user-service/.env.standalone
    ports:
      - "5432:5432"
    volumes:
      - user-db-data:/var/lib/postgresql/data
    networks:
      - tack-tracker-network

volumes:
  user-db-data:

networks:
  tack-tracker-network:
    driver: bridge
